# Big AI Team Project

This is a test README file for the Big AI Team project.

## Test Content

Adding test content to verify the pre-commit hook functionality.

## Project Structure

- `bridge/` - Bridge components
- `core/` - Core functionality
- `src/` - Source code
- `tests/` - Test files
- `scripts/` - Scripts and tools

## Getting Started

1. Ensure Docker is running
2. Run `deploy.bat` to build and run the project

## Testing

This project uses pytest for testing. Run tests with:

```bash
pytest
```

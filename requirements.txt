# ==========================================
# requirements.txt - 2026 工业级构建与分析环境
# ==========================================

# 1. 核心编译工具 (Nuitka 2.0+ 提供了更好的 C11/C++ 兼容性)
nuitka>=2.0.0

# 2. 极致压缩支撑 (Nuitka 自动调用 zstandard 压缩二进制体积，加速下发)
zstandard>=0.22.0

# 3. 静态代码分析与自动修复 (Ruff 0.3.0+ 支持最新的 Python 3.12+ 语法)
ruff>=0.3.0

# 4. 构建支撑（确保流水线中有符合现代标准的构建后端）
setuptools>=69.0.0
wheel>=0.42.0

# ------------------------------------------
# 如果你的核心逻辑依赖以下库，请根据需要取消注释：
# ------------------------------------------
numpy>=1.26.0      # 高性能数值计算
pandas>=2.2.0     # 数据处理
requests>=2.31.0   # 健壮的网络请求
pre-commit==3.5.0  # 代码提交前自动执行检查

opentelemetry-api
opentelemetry-sdk
redis>=4.0.0
opentelemetry-instrumentation-redis
opentelemetry-instrumentation-redis
opentelemetry-instrumentation-fastapi
opentelemetry-instrumentation-aiohttp-client